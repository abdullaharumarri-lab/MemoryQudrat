# خطة التنفيذ والتعديلات المطلوبة (Implementation Plan)

## الهدف
تلبية متطلبات المستخدم لتسهيل تجربة الاستخدام، وتوضيح الخيارات عند رفع الملفات، وتعديل سلوك "كويزاتي" و "مراجعات اليوم".

## التعديلات المقترحة (Proposed Changes)

### 1. تبسيط قائمة "كويزاتي"
- **الملف:** `handlers/main_menu.py`
- **التعديل:** في دالة `quiz_menu_keyboard`، سيتم إبقاء زرين فقط:
  - `▶️ ابدأ الكويز` (والذي سيعمل كـ "تجربة" `practice` فقط ولن يحسب في التكرار المتباعد).
  - `❌ حذف الكويز`.
  - إزالة زر "التجربة" المنفصل وزر "ابدأ الكويز" الخاص بالمراجعة، لأن المراجعة ستكون حصراً من قائمة "مراجعات اليوم".

### 2. خيارات جدولة المراجعة عند رفع ملف JSON
- **الملف:** `handlers/pdf_handler.py` & `database.py`
- **التعديل:** 
  - حالياً البوت يجدول المراجعة تلقائياً بناءً على الوقت (قبل أو بعد 6 م).
  - سيتم إيقاف الجدولة التلقائية.
  - عند رفع الملف، سيرد البوت برسالة تحتوي على زرين فقط:
    - `📅 ابدأ المراجعة اليوم`
    - `📅 ابدأ المراجعة غداً`
  - عند الضغط على أحد الزرين، يتم تحديد الموعد وإظهار رسالة تأكيد.

### 3. إظهار "مراجعات اليوم" قبل الساعة 6 مساءً كقائمة فقط (بدون أزرار حل)
- **الملف:** `handlers/main_menu.py`
- **التعديل:** 
  - في قسم `due_reviews`:
    - سيقوم البوت باستخراج الكويزات المستحقة لليوم (حتى قبل الساعة 6).
    - إذا كان الوقت قبل الساعة 6 م: سيعرض البوت قائمة بأسماء الكويزات المستحقة كنص فقط، مع رسالة واضحة: "المراجعات مقفلة وتفتح الساعة 6 م. سيصلك إشعار وقتها." (ولن يعرض أزرار الحل).
    - إذا كان الوقت بعد الساعة 6 م: سيعرض البوت أزرار الحل لكل كويز كالمعتاد.

### 4. الفرمتة النهائية
- بعد الانتهاء من جميع التعديلات البرمجية وتطبيقها بنجاح، سيتم تنفيذ أمر الفرمتة الجذري لمسح جميع البيانات السابقة وإعادة تشغيل البوت لتكون بداية نظيفة تماماً.

## أسئلة أو ملاحظات (Open Questions / User Review Required)
> [!IMPORTANT]
> **يرجى المراجعة:** هل أنت موافق على أن زر `▶️ ابدأ الكويز` داخل قائمة "كويزاتي" سيكون للتجربة فقط (لا يؤثر على خطة المراجعة) وأن المراجعة الحقيقية ستتم فقط من خلال قائمة "مراجعات اليوم" (بعد الساعة 6)؟ 
>
> إذا كانت الخطة مناسبة، الرجاء الموافقة للبدء بالتنفيذ!
