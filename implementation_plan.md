# خطة التنفيذ لميزة "إضافة كويز عبر رابط"

## 1. التعديل على قاعدة البيانات
سأقوم بإضافة عمود جديد `url` لجدول الكويزات (`quizzes`).
- الكويز العادي (JSON) سيكون قيمة الـ url فيه فارغة.
- الكويز الذي يضاف كرابط سيحتفظ بالرابط في هذا العمود، ولن يكون له أسئلة في جدول `questions`.

## 2. واجهة الإضافة
- سأضيف زراً جديداً في القائمة الرئيسية: **"🔗 إضافة رابط فورمز"**.
- عند الضغط عليه، سيطلب منك البوت إرسال الرابط.
- بعد إرسال الرابط، سيطلب منك إدخال "اسم الكويز".
- سيتم حفظ الكويز وجدولته فوراً في نظام التكرار المتباعد تماماً مثل كويز الـ JSON.

## 3. وقت المراجعة (كيف سيظهر لك)
عند الضغط على زر مراجعة كويز عبارة عن رابط، لن يفتح لك البوت نظام الأسئلة التفاعلي، بل سيرسل لك رسالة تحتوي على:
> 🔗 **اسم الكويز**
> للبدء في المراجعة، افتح الرابط وحل الكويز في المتصفح.

**ومعها 4 أزرار:**
1. `[ 🌐 افتح الرابط ]` (يفتح المتصفح مباشرة).
2. `[ ✅ تم الحل ]` (عندما تنهي الحل، تضغط عليه ليقوم البوت بجدولة المراجعة القادمة حسب التكرار المتباعد).
3. `[ 🔄 تحويل إلى JSON ]` (لترقية الكويز في المستقبل).
4. `[ 🔙 رجوع ]`.

## 4. ميزة "الترقية إلى JSON"
إذا قررت في أي يوم أنك تريد تحويل هذا الرابط إلى أسئلة تفاعلية داخل البوت (لتستفيد من تتبع الأسئلة الضعيفة)، يمكنك الضغط على زر **"تحويل إلى JSON"**.
- سيطلب منك البوت إرسال ملف الـ JSON الخاص بهذا الكويز.
- سيقوم البوت باستخراج الأسئلة وحفظها تحت نفس الكويز القديم!
- **النتيجة:** لن تخسر تقدمك أو جدولك في التكرار المتباعد، وسيكمل البوت جدولته معك كأنه كويز تفاعلي من البداية.

---
> [!IMPORTANT]
> **مطلوب مراجعتك:**
> هذه الخطة تلبي طلبك 100% وتحل مشكلة فقدان التقدم. هل أنت موافق عليها لنبدأ بالتنفيذ وكتابة الأكواد؟
