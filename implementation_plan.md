# خطة التنفيذ - تعديل المراجعات والتكرار المتباعد

## التعديلات
### 1. إصلاح فترات التكرار المتباعد
- **الملف:** `spaced_repetition.py`
- **التغيير:** تعديل `REVIEW_INTERVALS` لتصبح `[0, 1, 3, 7, 14, 30]`. 
  - المراجعة الأولى (Stage 0): نفس اليوم أو غداً (حسب اختيار المستخدم).
  - المراجعة الثانية (Stage 1): بعد 24 ساعة (1 يوم).
  - المراجعة الثالثة (Stage 2): بعد 3 أيام.
  - المراجعة الرابعة (Stage 3): بعد أسبوع (7 أيام).
  - المراجعة الخامسة (Stage 4): بعد أسبوعين (14 يوم).
  - المراجعة السادسة (Stage 5): بعد شهر (30 يوم).

### 2. المراجعات الفائتة (Overdue Reviews)
- **الملف:** `handlers/main_menu.py`
- **التغيير:** تعديل منطق زر "مراجعات اليوم":
  - أي مراجعة كان موعدها "أمس" أو قبل ذلك، ستظهر دائماً **مفتوحة** وجاهزة للحل كأزرار، حتى لو كان الوقت قبل الساعة 6 مساءً.
  - المراجعات المجدولة لـ "اليوم" فقط هي التي ستظل مقفلة حتى الساعة 6 مساءً.
  - إزالة العد التنازلي من هذه الشاشة لتجنب التكرار مع "جدول المراجعة". إذا كانت هناك مراجعات مقفلة لليوم، ستظهر رسالة بسيطة توضح ذلك.

### 3. زر مباشر في إشعار التذكير
- **الملف:** `bot.py`
- **التغيير:** إضافة زر مدمج `▶️ حل مراجعات اليوم` داخل رسالة التذكير اليومية التي تصل الساعة 6 م، ليأخذ المستخدم مباشرة إلى قائمة الكويزات المتاحة للحل.

### 4. الفرمتة
- بعد تطبيق جميع التغييرات البرمجية، سيتم تصفير قاعدة البيانات وإعادة تشغيل البوت لتطبيق الإعدادات الجديدة كلياً.
