# خطة التنفيذ للملاحظات المطلوبة

## 1. مشكلة جدول المراجعة (لا يعمل عند كثرة الكويزات)
- **السبب:** تليجرام يمنع إرسال رسالة يتجاوز طولها 4096 حرفاً. عند كثرة الكويزات، يصبح نص الجدول طويلاً جداً فيرفض تليجرام تعديل الرسالة ويبدو الزر وكأنه معلق.
- **الحل:** سأقوم ببرمجة **نظام صفحات (Pagination)** لجدول المراجعة. بحيث يعرض مثلاً 5 كويزات في كل صفحة، مع أزرار (التالي ⬅️) و (السابق ➡️) للتنقل بين الصفحات بسهولة.

## 2. الأسئلة الضعيفة والتكرار المتباعد
- **السبب الحالي:** الزر حالياً يعرض فقط الأسئلة الضعيفة **المستحقة للمراجعة اليوم**، ولا يعرض جميع الأسئلة التي أخطأت فيها.
- **الحل:** سأقوم بتعديل زر "الأسئلة الضعيفة" ليعرض قائمة بجميع الكويزات التي تحتوي على أخطاء (سواء كانت مستحقة اليوم أم لا). وعند الضغط على كويز، سيظهر لك خياران:
  1. مراجعة الأسئلة المستحقة فقط (حسب التكرار المتباعد).
  2. التدرب على **جميع** الأسئلة الضعيفة في هذا الكويز (بدون التأثير على التكرار المتباعد).

## 3. اقتراح لحل مشكلة الخيارات الطويلة جداً
كما طلبت، هذا مجرد اقتراح ولن أطبقه إلا بموافقتك:
بدلاً من وضع النص الطويل داخل الزر (والذي يؤدي إلى قصه أو تشويهه)، يمكننا عرض الخيارات داخل نص الرسالة نفسها بحروف أبجدية، وتكون الأزرار عبارة عن رموز فقط.
**مثال لشكل السؤال:**
```
س: ما هي أبرز نتائج الثورة الصناعية في أوروبا خلال القرن التاسع عشر؟

أ) زيادة الإنتاج الزراعي بشكل ملحوظ بسبب استخدام الآلات... (نص طويل)
ب) تراجع النمو الاقتصادي بسبب قلة الأيدي العاملة... (نص طويل)
ج) ظهور الطبقة العاملة وتحسن بعض ظروف المعيشة... (نص طويل)
د) انخفاض معدلات التلوث بسبب الاعتماد على الفحم... (نص طويل)
```
**الأزرار أسفل الرسالة ستكون هكذا:**
`[ أ ]` `[ ب ]` `[ ج ]` `[ د ]`

هذا الحل سيجعل القراءة مريحة جداً مهما كان الخيار طويلاً. (أخبرني إذا كنت تريدني أن أعتمد هذا الحل).

## 4. النسخة الاحتياطية
سأقوم بنسخ قاعدة البيانات الحالية وتسميتها `memory_qudrat_backup.db` على سيرفرك كنسخة آمنة، ولن أقوم بفرمتة أي شيء.

---
> [!IMPORTANT]
> **يرجى المراجعة:** 
> 1. هل أنت موافق على خطة تقسيم جدول المراجعة إلى صفحات؟
> 2. هل أنت موافق على خطة زر الأسئلة الضعيفة؟
> 3. ما رأيك في اقتراح (أ، ب، ج، د) للخيارات الطويلة؟ هل أطبقه؟
